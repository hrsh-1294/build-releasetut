# Simple workflow for deploying static content to GitHub Pages
name: Python Script Worlflow

on:
  # Runs on pushes targeting the default branch
  push:
    branches:
      - main

jobs:
  run_python_script:
    name: Run script.py
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set Up Python
        uses: actions/setup-python@v2
        with:
          puthon-version: '3.12' #use the python version you need

      - name: Run script.py
        run: python script.py

  run_python_script_2:
    needs: run_python_script
    name: Run script2.py
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set Up Python
        uses: actions/setup-python@v2
        with:
          puthon-version: '3.12' #use the python version you need

      - name: Run script2.py
        run: python script2.py
